;;; package --- Summary

;;; Commentary:

;;; Code:
;; x => *** Eval error ***  Symbol’s value as variable is void: x
;; (x) => *** Eval error ***  Symbol’s function definition is void: x
(setq ct 0)
(setq ct (+ ct 2))
(message "Hello World")
;;; ch1.el ends here
